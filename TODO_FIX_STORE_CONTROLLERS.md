# TODO (Store)

- ✅ Added real Admin/Store CRUD UI for **Stock Items (Items table)** (frontend)
- ✅ Wired sidebar path `/shared/store/stock/items` to working page
- ✅ Hooked CRUD UI to existing APIs:
  - `GET /store/items`
  - `POST /store/items`
  - `PUT /store/items/:id`
  - `DELETE /store/items/:id` (deactivate)

Notes:
- Backend already had these item endpoints in `hms-backend/routes/storeRoutes.js`.

